name: Unit Tests
on:
  pull_request:
    paths:
      - '**/*.cs'
      - '**/*.csx'
      - '**/*.csproj'
      - '**/*.sln'
      - '**/*.props'
      - '**/*.targets'
  push:
    branches: [ main, dev ]
    paths:
      - '**/*.cs'
      - '**/*.csx'
      - '**/*.csproj'
      - '**/*.sln'
      - '**/*.props'
      - '**/*.targets'

jobs:
  unit-tests:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run unit tests
        run: |
          cd "Tests\WolvenKit.UnitTests"
          dotnet test